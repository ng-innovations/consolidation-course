{
  "name": "consolidation-course-server",
  "version": "0.1.0",
  "private": true,
  "description": "Microsoft SSO gate for the consolidation course static content. Code is a copy of static-sso-shell — re-copy from there when the shell is updated.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "NODE_ENV=development node server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "axios": "^1.7.7",
    "dotenv": "^16.4.5",
    "express": "^4.21.1",
    "express-session": "^1.18.1"
  }
}
