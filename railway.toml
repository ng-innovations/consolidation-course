# Build & deploy config for Railway / Nixpacks.
# Nixpacks detects Node from the root package.json (which only exists to
# trigger Node detection — actual server code is in ./server/).
# The root package.json's "build" and "start" scripts delegate to server/.

[build]
builder = "nixpacks"

[deploy]
restartPolicyType = "always"
